slam_toolbox:
  ros__parameters:
    use_sim_time: true
    mode: "mapping"

    # frames
    map_frame: "map"
    odom_frame: "odom"
    base_frame: "base_link_icp"

    # topics
    scan_topic: "/scan"

    
    transform_timeout: 0.5
    tf_buffer_duration: 30.0